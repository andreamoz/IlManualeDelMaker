// le righe che cominciano con // sono dei commenti
void setup() {
  // qui ci va il codice di inizializzazione
  // viene eseguito una sola volta
}
void loop() {
  // qui ci va il codice principale
  // viene eseguito in continuazione
}
